{
  "project": "next-web-boilerplate",
  "packageManager": "pnpm",
  "gate": ["pnpm lint", "pnpm type-check", "pnpm build"],
  "test": {
    "unit": "pnpm test",
    "e2e": "pnpm test:e2e"
  },
  "verify": {
    "build": "pnpm build",
    "run": "pnpm --filter web start --port 3100",
    "ready": { "kind": "http", "port": 3100 },
    "notes": "Use the no-`--` form of `run` — verified 2026-09-02 by running both. `pnpm --filter web start -- --port 3100` forwards the `--` LITERALLY into the script's argv (`next start \"--\" \"--port\" \"3100\"`), and `next start` then reads `--port` as the project directory: 'Invalid project directory provided, no such directory: …/apps/web/--port'. The no-`--` form yields `next start \"--port\" \"3100\"` and serves on :3100. Root .env is loaded by the start script's dotenv wrapper (confirmed: /api/health returns database:up). Auth origin checks are exact — send an Origin header matching the served URL. Full recipes live in project memory."
  },
  "prodVerify": {
    "build": "pnpm build",
    "start": "pnpm --filter web start --port 3100",
    "port": 3100,
    "notes": "v1 fallback, kept until the kit drops it. Same verified no-`--` command as `verify.run` — do NOT reintroduce `-- --port 3100`, which makes `next start` treat `--port` as the project directory."
  },
  "cache": {
    "size": "pnpm cache:size",
    "prune": "pnpm cache:prune"
  },
  "docs": {
    "dir": "docs",
    "status": "docs/PROJECT_STATUS.md",
    "backlog": "docs/BACKLOG.md",
    "contextDir": "docs/context",
    "archiveDir": "docs/archive"
  },
  "commit": {
    "style": "conventional",
    "trailers": false,
    "useFileFlag": true
  },
  "ci": {
    "provider": "github-actions",
    "workflow": "CI"
  },
  "hygiene": {
    "devPorts": [3000, 3100],
    "e2eUserPattern": "@example.com",
    "keepContainers": ["nwb-postgres", "nwb-meilisearch"]
  },
  "depPolicy": {
    "releaseAgeDays": 7,
    "exactPin": [
      "lint-staged",
      "lucide-react",
      "stripe",
      "@sentry/*",
      "posthog-*",
      "react-email",
      "@axe-core/playwright",
      "better-auth"
    ]
  },
  "contextBudget": {
    "agentsMdMaxLines": 150,
    "contextDocSplitTokens": 3000,
    "memoryIndexMaxTokens": 700,
    "memoryFileMaxTokens": 1500,
    "skillDescriptionMaxTokens": 909
  },
  "init": {
    "scaffold": "node scripts/init-app.mjs --slim --name {name}",
    "productBrief": "docs/PRODUCT.md",
    "migrationMap": "docs/MIGRATION.md",
    "sourceDir": "intake/source/"
  }
}
