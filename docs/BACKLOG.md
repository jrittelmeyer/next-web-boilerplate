# Backlog — Forward (Watch + Tier 4 upgrade paths)

> **Forward-only backlog** (formerly `PHASE_3_IDEAS.md`). Phases 1–5 and every
> locally-buildable Tier-4 row are complete and pushed to main. Shipped-item detail is
> **not** kept here: the compact record is the per-program summary in
> [PROJECT_STATUS.md](PROJECT_STATUS.md) (also the only home of the audit score
> litany), and the full per-item prose is in
> [archive/PHASE_HISTORY.md](archive/PHASE_HISTORY.md). The audits that seeded past
> backlogs live in [docs/archive/](archive/) (Phase B + the seventeen `/project-audit`
> scoring passes). Everything below goes plan → sign-off → build. Don't reintroduce
> shipped-item entries here.

## Watch (no action now)

Full detail + removal conditions: [docs/MAINTENANCE.md](MAINTENANCE.md) (canonical).

- **TypeScript 7 cutover** — GA'd (`typescript@7.0.2`) but ships no JS Compiler API. The Next-side gate lifted 2026-08-02 (`experimental.useTypeScriptCli` in stable `next@16.2.12`), **but that is no longer the binding constraint**: ⚠️ **TS 7 ships no `tsserver`**, so a cutover would leave the editor and the `next` tsserver plugin without a language service — and no CI lane can observe that, which is why the "run a trial" re-gate was retired on 2026-08-02 in favour of a checkable one. **Re-gated on TS 7.1** (~Q4 2026). Hold Renovate's `typescript` v7 major.
- **Calendar Phase 6 — sharing & interop** — the calendar's next program. Not started, and until 2026-08-02 not tracked anywhere despite ~30 in-code and in-doc "Phase 6" commitments; see the B2 row below.
- **Calendar reminders — three accepted limits** (end-anchored reminders · guest reminders · reader-zone email rendering), plus the sweeper's fixed 60-minute look-back as a deployment knob. Each was recorded in a doc or a source comment and tracked nowhere. ⚠️ Dropping the `anchor` CHECK **alone** yields a reminder that silently never fires.
- **A global `now` for relative-time formatting** — deferred and self-gating; no route server-renders a `relativeTime` yet. Add it alongside the first one.
- **Maintenance-only (Tier 3 G)** — the standing state since 2026-07-17 (verified 100.0/100). **Renovate delivery — the picture changed 2026-08-31:** after a month of empty Monday windows, the self-hosted `renovate.yml` fallback shipped (B1 row below), its first cron run failed for want of the `RENOVATE_TOKEN` secret, and the same morning the Mend App opened the first scheduled `renovate/*` PR ever (#56, CI green). Which host stays is the owner's call — options + evidence in [MAINTENANCE.md](MAINTENANCE.md). The `ENABLE_RENOVATE` gate that makes `renovate.yml` template-safe **shipped 2026-09-02**; enabling now takes two actions (secret **and** variable), so a dated 14-day liveness check rides with it in [MAINTENANCE.md](MAINTENANCE.md) — a forgotten variable reproduces the same silent zero-PR observable that hid the Mend failure for six weeks.
- **Three distinct e2e defects — and the lane has gone red twice (2026-08-03, PR #34 attempt 1: 1 failed · 9 flaky · 56 passed; 2026-09-01, `3e68733` attempt 1 — the deterministic month-boundary defect, (c) in MAINTENANCE and a B2 row below).** They are separate defects and the old single row conflated them; full detail + removal conditions in [MAINTENANCE.md](MAINTENANCE.md). **(a) The signup hang — DIAGNOSED and FIXED 2026-08-03** (`e2e/support/auth.ts`): the helper clicked "Create account" **before React hydrated the form**, so no request was ever issued and `waitForURL` hung on a page that had submitted nothing. Reproduced 8/8 under 6× CPU throttling; the CI log agrees (hung attempts left no server-side signup line, so the account was never created). Fixed by settling the form, then awaiting the auth response beside the click and asserting its status. ⚠️ **Two hypotheses were tested and RULED OUT** — a Next `router.push`+`refresh` race (30/30 clean on 16.2.12, and the missing server line disproves its required shape) and the 5/60s rate limiter (zero 429s in the failing log). Don't revive either without new evidence. **(b) The `set-active` hang** (`e2e/organization.spec.ts:43`) — this is what died at Retry #2 and turned the lane red. Root cause still **unknown**; the 2026-08-03 change removed `r.ok()` from its predicate so a non-2xx names itself instead of hanging, and made all three Playwright lanes produce reports + traces. **Next red is the evidence — read the artifact before theorising.**
- **Temporary security overrides** — ten `overrides:` in `pnpm-workspace.yaml` (nine security + the `vite` freshness pin), each key carrying its own dated why-comment and removal condition in that file; `auditConfig.ignoreGhsas` is `[]` (zero ignored, `pnpm audit` clean) as of 2026-08-26. The park/exit machinery and the three-route rule live at `minimumReleaseAge` in that file + [MAINTENANCE.md](MAINTENANCE.md) (canonical). Current edge: `nanoid` closed at `"nanoid@<3.3.18": 3.3.18` (taken 2026-08-14) and the `sharp` override retired 2026-08-26 with the `next` 16.3.3 take (its removal condition — next's own sharp pin ≥0.35.0 — met; see [MAINTENANCE.md](MAINTENANCE.md) · [CHANGELOG](../CHANGELOG.md)). `minimumReleaseAgeExclude` is **empty again as of 2026-09-02** — the `next` 16.3.3 ten-entry block was deleted on schedule once 16.3.3 aged past the 7-day gate, so the gate is unconditional with zero exclusions. Full history (the fast-uri graduation, the dompurify/nanoid parks, the two dated age-excludes): [archive/PHASE_HISTORY.md → Archived 2026-08-19](archive/PHASE_HISTORY.md#archived-2026-08-19-status-watch-and-overrides-history).
- **`contrarian` subagent — evaluated 2026-07-28, kept** — it cleared its acceptance bar twice (incl. catching its own `Bash` grant contradicting a "read-only" description). Kill criterion committed: three consecutive merged ALWAYS-path PRs with no `## Contrarian disposition` PR-body section ⇒ the policy is dead. Registration is **surface-dependent** (a reload does not fix it) — fallback recipe: [CONVENTIONS.md → Agent tooling](context/CONVENTIONS.md#agent-tooling-claude).
- **`main` has no branch protection** — `gh api …/branches/main/protection` → 404, so no status check is actually required. Every merge-ordering discipline is self-imposed. Owner decision, not a build row.
- **Ship a real derived product end-to-end** — owner-driven, in flight (via `/project-init`); unlocks the gated B1 intake-drop row and feeds the on-ramp rows with real lessons.

## Tier 4 — Future upgrade paths (documented, unscheduled)

> Each open row is a real direction, **opt-in / on real need** (the starter is
> feature-complete without them), and goes plan → sign-off → build. Shipped rows keep one
> strikethrough line in the table at the bottom — the record is the PROJECT_STATUS
> summary + the doc in "See"; don't re-expand them here.
>
> **The path-to-100 program** (2026-07-15, owner-directed) — 11 rows recovering the 13
> audit points locked behind won't-fix/deferred classifications (per-row re-analysis in
> [archive/PATH_TO_100_2026-07-15.md](archive/PATH_TO_100_2026-07-15.md)) — **shipped all
> 11 build rows 2026-07-16 → 17** (rows archived →
> [archive/PHASE_HISTORY.md](archive/PHASE_HISTORY.md)), closed the last remainder
> (**#4b**, the one-time live Uploadthing tunnel proof, 2026-07-17), and was **VERIFIED
> at 100.0/100 by the 2026-07-17 scoring pass**
> ([archive/PROJECT_AUDIT_2026-07-17.md](archive/PROJECT_AUDIT_2026-07-17.md)). The
> **TypeScript 7 cutover** stays outside the program (externally gated, costs no
> points; see Watch). The 2026-07-22 pass scored **99.65** — the first drop since,
> and none of it code — and all four of its rows shipped same-day (rows archived
> likewise); the audit ledger is clear again
> ([archive/PROJECT_AUDIT_2026-07-22.md](archive/PROJECT_AUDIT_2026-07-22.md)). The
> twelfth pass (2026-07-29) verified F2–F4 closed and scored **99.9** — F1 (Renovate
> delivery) is the sole open deduction; its diagnosis row is in Open rows
> ([archive/PROJECT_AUDIT_2026-07-29.md](archive/PROJECT_AUDIT_2026-07-29.md)). The
> **thirteenth pass (2026-08-04) scored 98.6** — the first to score the calendar
> program (new group, enters at 85), plus the live 9-advisory batch (issue #41) and
> the red `main` both fix-pending; it seeded every `audit-2026-08-04` row below
> ([archive/PROJECT_AUDIT_2026-08-04.md](archive/PROJECT_AUDIT_2026-08-04.md)). The
> **fourteenth pass (2026-08-06) re-scored 99.3** — its F4–F8 verified fixed under
> adversarial re-review (calendar 85 → 95.5), the live surface fully green; zero new
> rows, two edited below
> ([archive/PROJECT_AUDIT_2026-08-06.md](archive/PROJECT_AUDIT_2026-08-06.md)). The
> **fifteenth pass (2026-08-13) re-scored 99.3** — the 08-08 closures verified genuine;
> its two pipeline findings seeded the kit-gate B1 row below, whose repo half closed
> 2026-08-14 ([archive/PROJECT_AUDIT_2026-08-13.md](archive/PROJECT_AUDIT_2026-08-13.md)).
> The **sixteenth pass (2026-08-19) re-scored 99.4** — the fifteenth's prescriptions
> all verified executed (Security → 100, Testing → 99, `main` green); **zero new
> rows**; its one drift find (the better-auth line moved to a breaking 1.7 `latest`
> with no watch bullet) became a MAINTENANCE dated take
> ([archive/PROJECT_AUDIT_2026-08-19.md](archive/PROJECT_AUDIT_2026-08-19.md)). The
> **seventeenth pass (2026-09-01) re-scored 99.3** — the 08-26 takes, the CVE fix and
> the v1.2.0 cut verified at their seams; **three new rows** (the fork-safe
> `renovate.yml` gate, the month-boundary e2e fix, the two draft releases) and a
> reference sweep that caught STACK.md's version table left stale by both takes;
> contrarian 13/13 folded
> ([archive/PROJECT_AUDIT_2026-09-01.md](archive/PROJECT_AUDIT_2026-09-01.md)).

### Open rows

| Band | Area | Upgrade | Documented in | Notes |
| --- | --- | --- | --- | --- |
| B4 | Toolchain | **TypeScript 7 cutover** (outside the program) | STACK.md | The Next-side gate LIFTED 2026-08-02 (`experimental.useTypeScriptCli` in stable `next@16.2.12`, verified in the installed artifact) — **but the binding constraint turned out to be something no cutover trial could see.** ⚠️ **TS 7 ships no `tsserver`** (`bin: { tsc }` vs TS 6's `{ tsc, tsserver }`): the editor's "Use Workspace Version" and the **`next` tsserver plugin** (`tooling/typescript/nextjs.json`) would have no language service, so the editor would check with a *different compiler than the build* — and **no `ci.yml` lane runs an editor**. Two further costs land on the **template surface**, which `init-app.mjs` ships verbatim: TS 7 is a native binary for 20 tuples with **no musl variant** (the builder is `node:24-alpine`), and `next.config.ts`'s `experimental` key exists only in nonce mode, so the flag must be *merged* into it, not added beside it. **The 2026-08-02 "run a trial" re-gate is RETIRED** — a green trial would not have licensed the cutover. ***New removal condition:*** `typescript@7.1.x` stable **and** a language service exists **and** `react-docgen-typescript` resolves against it. Cutover mechanics, corrected: the bump is **10** specifiers, not 9 (`packages/calendar` postdates the earlier note), and needs **no** `minimumReleaseAgeExclude` — but should pin **exactly**, since the gate binds what a range resolves to and TS publishes daily. Full detail in Watch above. Costs no audit points. |
| B2 | Calendar | **Phase 6 — sharing & interop** — calendar sharing + org calendars, ICS feed/import, per-occurrence RSVP, guest permissions, inbound iTIP `REPLY` ingestion, `VTIMEZONE` synthesis, and invitations appearing on the invitee's month grid | [context/calendar/](context/calendar/model.md) · master plan (local) | Phases 0–5 shipped 2026-07-30 → 08-02. **Filed 2026-08-02 by the doc audit**: ~30 "Phase 6" commitments exist across `docs/context/calendar/*`, `DECISIONS.md`, `packages/db/src/schema/*`, `packages/validators`, and the tRPC calendar router, and **none of them had a tracked home** — an agent reading this forward-only backlog concluded the calendar was finished. `MAINTENANCE.md`'s cascade-moved-override Watch item even sets its *removal condition* on Phase 6 landing. Inherits Phase 5's three recorded debts (end-anchored reminders, guest reminders, reader-zone email rendering) — see the Watch entries. ⚠️ Read **`ACL`** first: `calendar.range`'s owner-scoped `WHERE` must move behind `getCalendarRole` in the same change as any share resolver, or shared calendars authorize on the write path and **silently return nothing** on the read path. **Sliced 2026-08-02** (owner decision) into two bands, because the row is the deferral bucket of Phases 3–5 rather than a phase: **Band 1** — discharge what the repo already asserts and does not do — is **DONE** (the recurring-span grid defect; the audit, `private`-visibility and reminder-interlock statements; the doc corrections). **Band 2** — new template surface: `6A-0` a behaviour-preserving `listVisibleCalendarIds` refactor (no migration), then shares-only `6A`, `6A-org`, `6B` invitations-on-grid, `6C` feed + `VTIMEZONE` + change detection, `6D` ICS import, `6E` guest permissions + per-occurrence RSVP — is **not started and not authorized**; gate it on a real consumer needing sharing. ⚠️ `6E` must be re-planned against `DECISIONS.md`'s ruling that per-occurrence RSVP is *"a privilege-escalation shape"*, **not** the parent program's "needs zero new schema". **Out of scope by decision:** inbound iTIP `REPLY` ingestion (no inbound-email transport here; mirrors the Google/Microsoft sync call) and guest reminders (need a consent surface — `email_suppressions` is a bounce list, not consent). Plan → sign-off before build. |
| B1 | Tooling / deps | **Restore Renovate PR delivery** — `.github/workflows/renovate.yml` built 2026-08-31 (self-hosted `renovatebot/github-action`, SHA-pinned, reuses `.github/renovate.json` unchanged) after diagnosis at developer.mend.io ruled out every repo-side and account-side cause (App installed, Interactive mode, correct schedule evaluation) and a manual trigger's job log (`ea8d7e50`) showed the run killed mid-`pnpm update` with no error emitted — a Mend Community/Free-tier resource ceiling, not a config defect | [MAINTENANCE.md → Automation on a fork](MAINTENANCE.md#automation-on-a-fork--new-repo) · [diagnosis](archive/renovate-b1-diagnosis-plan.md) | **Owner decision pending (2026-08-31):** the first cron run (18:28 UTC) failed at startup — `'token' MUST be passed … RENOVATE_TOKEN`; `gh secret list` shows no repo secret — and the same Monday the Mend App opened [#56](https://github.com/jrittelmeyer/next-web-boilerplate/pull/56) (`actions/checkout` 7.0.1, every lane green), the first scheduled `renovate/*` PR ever: Mend does deliver the no-lockfile class (the Dependency Dashboard's `updatedAt` is still 2026-07-22, so its run still never finishes). Pick **one** host — (a) add `RENOVATE_TOKEN` (classic PAT, `repo` + `workflow`) and uninstall the Mend App, per the plan; or (b) keep Mend, delete `renovate.yml`, and accept that lockfile-bearing PRs may keep dying on Mend's tier. Never both: the duplicate-dashboard race in the workflow header is live now. Then merge/close #56 (10/10 checks green as of 2026-09-01; it touches `ci.yml`/`codeql.yml`/`pages.yml`/`security-audit.yml` — **not** `renovate.yml`, which postdates it and keeps `actions/checkout` v7.0.0; the `workflow` scope is needed) and confirm a scheduled `renovate/*` PR from the chosen host; move to Shipped once observed. Independent of the choice, and now **shipped** (2026-09-02): the fork-safe `ENABLE_RENOVATE` gate, so a generated project no longer inherits the weekly red — but enabling here is now a **two**-action close (secret **and** variable), tracked by a dated 14-day liveness check in MAINTENANCE. Evidence 2026-09-01: 58 outdated (66 on 08-19); exact-pinned `posthog-js` 32 minors behind, `@sentry/nextjs` 14, `knip` 10, `stripe` 4. |
| B1 | On-ramp / kit | **Intake-drop convention for `/project-init`** — template half: seed a committed `docs/intake/` (README: drop planning docs here → run `/project-init`) + a GETTING_STARTED sentence + init-app kept-list mention; kit half: `init.intakeDir` adapter field (default `docs/intake/`), intake enumeration in project-init §1, raw docs → `docs/archive/product-intake/` in the inception commit after brief sign-off (prevents a second source of truth beside `PRODUCT.md`) | [GETTING_STARTED.md](GETTING_STARTED.md#starting-from-an-idea-run-project-init) | Direction owner-approved 2026-07-18; **build after the first real derived-project inception run (in flight) supplies lessons.** Kit half edits an ai-dev-kit clone → re-install (`--dest`), never the installed copies. Verified: init-app `--slim`'s delete list doesn't touch `docs/intake/`. Sibling convention shipped 2026-07-19: `intake/source/` (gitignored **code** drop for `/project-adopt`) stays separate — committed planning docs vs never-committed source. Plan → sign-off before building. |
| B3 | Docs / positioning | **README / tagline reframe around the agent-native workflow** (OWNER-DIRECTED) — lead with the real differentiator: the context-doc system + working agreements + verification culture + ai-dev-kit's two inception doors, not the wiring | README.md · AGENTS.md | Dozens of starters have the wiring; nothing else has the operating system around it, and today it's buried in AGENTS.md / the docs. This is framing/marketing judgment — needs an owner decision, not a mechanical build. Pairs with the visual surface + the derived-product proof. |
| B3 | Docs / showcase | **Slide deck has no calendar coverage** — no feature-table row, no slide, and the hand-curated 12-point chart carries no scheduling point; before 2026-08-03 the deck contained the string "calendar" **zero** times | [slide-deck.html](plain-english-guide/slide-deck.html) · [guide ch. 12](plain-english-guide/12-the-calendar.md) | **Found 2026-08-03 while writing guide ch. 12.** A currency defect, not a gap: the 2026-08-02 showcase pass (4/4) re-stamped the deck and added a 12th chart point without noticing the repo's largest feature program was absent from it entirely. The ch. 12 change fixed the deck's chapter links and named the calendar in its chapter-list footer — deliberately **link/label only**, since deck slides are owner-voice pitch material rather than explanatory prose. ⚠️ **The deck is HTML, so `docs:sanity` link-checks none of it**: every deck → guide link is unguarded and was verified by hand. Plan → sign-off before build. |
| B1 | Kit | **Second ai-dev-kit adapter (portability proof)** — author an adapter for a different stack to exercise the kit's stack-agnostic claim end-to-end | ai-dev-kit repo (`adapters/`) · [CLAUDE.md](../CLAUDE.md) | **Recommend waiting for a real second-stack project to pull it** — an adapter with no consuming repo is unverifiable. On real need. |
| B1 | Permissions | **Least-privilege pass on the tracked `.claude/settings.json`** — remove `Bash(winget install *)`, `Bash(docker exec *)`, the dead `dotenv-cli` install grant and the dead literal-commit grant; add `Bash(pnpm lint *)` + the verify lane's worktree-local commands and their PowerShell twins; add a `deny` tripwire (force-push in both flag orders incl. `--force*`; `Read(.env)`, `Read(.env.local)`, `Read(.env.*.local)` by gitignore-semantics bare names) | [harness audit 08-31](archive/HARNESS_AUDIT_2026-08-31.md) · [.claude/settings.json](../.claude/settings.json) | Filed 2026-08-31 (harness audit, Permissions 82/100). All eight grants date from the 2026-07-14 release commit; the two broad ones ship auto-approved into every generated project. Pre-merge: run the `.env` sourcing and `grep\|cut` recipes under the deny rule (`Read` denies reach `cat`/`head`/`tail`/`sed`); fall back to `pnpm exec dotenv -e .env -- <cmd>`. `ask: Bash(git push *)` considered and rejected (would prompt the autonomous checkpoint push); deny beats allow from every scope, so there is no session-level lift. Template surface ⇒ contrarian + sign-off. Also lifts the project audit's Monorepo & tooling +0.5 (2026-09-01, F6). Effort S. |
| B3 | Tooling / CI | **Kit-drift gate, fork-safe** — a static-lane step gated `if: vars.ENABLE_KIT_DRIFT == 'true'` (the `ENABLE_*` convention; ON here, unset in generated projects) that resolves `v<installed.json.kit>`, **skips with a warning when the tag is missing**, else fetches the kit at that tag and runs `install.mjs --check --dest .` | [harness audit 08-31](archive/HARNESS_AUDIT_2026-08-31.md) · [ci.yml](../.github/workflows/ci.yml) | Filed 2026-08-31. Today `--check` is manual and `skill-drift-guard` advisory, so an in-place edit under `.claude/skills/` survives until someone runs the installer. Ask the kit for an install SHA in `installed.json` as the tag-free alternative. Workflow surface ⇒ contrarian + sign-off. Effort S. |
| B3 | Tooling / lint | **Gate the prose-only hard rules, lint-only** — a probe with `@ts-ignore`, `as any`, `enum`, `useMemo` passes `biome lint` with one warning; `noExplicitAny: error` (note: bans `: any` too), Biome 2 `noTsIgnore` + `noEnum` (verify names), and ESLint core `no-restricted-imports` on `react`'s `useMemo`/`useCallback`/`memo` in `tooling/eslint/next.js` | [harness audit 08-31](archive/HARNESS_AUDIT_2026-08-31.md) · [AGENTS.md](../AGENTS.md) | Filed 2026-08-31. Step 1 is a decision on the five sites that already violate the Compiler rule (`notifications-feed.tsx:103,111,120` with a WHY; `use-consent.ts:47,52`): remove, or suppress inline with the reason. One `as any` in a test (`calendar-rsvp.test.ts:144`). `enforcement.bannedApis` **overruled** by contrarian — whole-file scan, exit 2, path-level exemptions only, and CONVENTIONS' no-`enforcement` rule is absolute. Step 1 is neutral per site: `notifications-feed.tsx`'s query-key memo is a referential-stability argument (effect deps + `setQueryData` exact-match) the Compiler does not guarantee, so "record the exception with its WHY" is a legitimate outcome there. Also lifts the project audit's Framework +0.5 (2026-09-01, F4). Effort S–M. |
| B2 | Testing / e2e | **Month-boundary fix in `apps/web/e2e/calendar-invitations.spec.ts`** — `dayInThisMonth()` (`:54-58`) derives the event's month from the **runner's** clock while the organizer's stored zone is `America/New_York` (`:73`) and `/calendar` opens on today in the *stored* zone (`calendar/page.tsx` → `instantToCivil(Date.now(), preferences.timeZone)`); derive the day in `EVENT_ZONE` via `Intl.DateTimeFormat("en-CA", { timeZone: EVENT_ZONE })` and make `now` injectable (or unit-test the helper) so the discrimination proof runs on any day, not only near a boundary | [MAINTENANCE.md → Watch (c)](MAINTENANCE.md#watch-items-known-tracked-deliberately-not-done) · [audit 09-01 F3](archive/PROJECT_AUDIT_2026-09-01.md) | Filed 2026-09-01 from the `3e68733` attempt-1 red (09-01 02:37Z: `:165` never found the "Standup" chip — the grid showed August). Deterministic in the window **00:00–04:00 UTC on the 1st of every month** (to 05:00 in EST — a winter 1st that is a Thursday puts the 04:30Z heartbeat inside it); `apps/web/e2e` ships to generated projects, so their lanes inherit the window too. Do **not** switch to `calendar.spec.ts`'s fixed-2027-date + `gotoMonth` pattern: this spec visits `/calendar` six times and ~7 arrow presses per visit would trip the 20/min `calendar.range` cap the header (`:29-30`) already avoids. `a11y.spec.ts:157`'s UTC-month seed is **verified safe** (`DEFAULT_TIME_ZONE = "UTC"`, `lib/user-preferences.ts`; that user stores no zone) — not part of this row. Test-only; resets the 20-green flake counter honestly. Land before 2026-10-01 00:00Z. Testing +1. Effort S. |
| B2 | Calendar / UX | **Render `calendar.range`'s error state** — a 429 (20 reads/min; 24 month-arrow presses) currently paints an empty month indistinguishable from "no events"; reconsider the bucket for paging while at it | [MAINTENANCE.md → Watch](MAINTENANCE.md#watch-items-known-tracked-deliberately-not-done) · [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) | Promoted from Watch (its own text calls the blank grid "the part worth fixing"). Calendar +1. Effort S. |
| B2 | Email / i18n | **Localized reminder emails for account holders** — pass locale + `user_preferences.timeZone` through the sweeper to the template; today every reminder is en-GB in the *event's* zone even when the reader's is known | [MAINTENANCE.md → Watch](MAINTENANCE.md#watch-items-known-tracked-deliberately-not-done) · [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) | Promoted from Watch (removal condition pre-specified there). Email +1. Effort M. |
| B3 | Testing | **Predicate-sensor long tail** — the 2026-08-08 sweep inventory's remaining weak/no-sensor WHERE-conjunct rules. Headline: per-user reminder scoping (`calendar.ts:668-670` + tRPC `:593-598`) is the one *destructive* gap — the composer seeds from the read and the next save deletes what it didn't see; needs a two-user shared-event fixture, or an interim `@repo/db` restate. The rest: calendar-rsvp's deleted-event UPDATE guard (the EXISTS half; the landing page is now covered) · primary-calendar demote `ne()` self-exclusion · `splitSeries`/`truncateSeries` `gte` cut edges · `skipOccurrence` pair · `removeAttendees` event scope · override-under-deleted-master, occurrence-detail and calendar-list-scope reads · `markAllRead` owner arm + unread-count cross-user arm · data-export audit `or()` completeness · expired-session listing + OAuth-only password card · keyset same-timestamp tiebreak (5 sites) · duplicate-title org arm · org role lookup | [audit 08-06 F2](archive/PROJECT_AUDIT_2026-08-06.md) · [TESTING.md → Predicate sensors](context/TESTING.md) | Filed 2026-08-08 by the B2 sweep. None user-visible on a standard deploy today; tRPC routers carry zero unit sensors **as a class** — that evidence feeds any future harness decision. Close opportunistically when touching a site (the two TESTING.md patterns are the toolkit), or batch on real need. Effort M. |
| B3 | Calendar | **Long-tail correctness batch** — RFC 6868 param quoting (+ `:` in CN) and mailto hygiene in the ICS serializer; DATE-form `UNTIL` zone semantics; `seriesEndInstantMs` fall-back slack + COUNT `truncated` bit; DAILY+BYMONTHDAY (support it or fix the false RFC attribution); one-off RSVP tokens expire from `endAt`; `updateOccurrence` membership check; actor-self cancellation in the mixed unverified+configured state (decide, and fix `softDeleteEvent`'s contradicted "every guest but the deleter" comment); `loadRecipients`' organizer-exclusion comment vs its filterless SELECT (decide comment or filter) | [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) (F10) · [audit 08-06 F5](archive/PROJECT_AUDIT_2026-08-06.md) | Nine smalls, none user-visible on a standard configured deploy (organizerName always null; UI never emits DATE-UNTIL; the self-cancellation needs the mixed state). Calendar +2.5. Effort M. |
| B3 | UI / a11y | **`@repo/ui` table-container remedy** — `tabIndex={0}` + `role="region"` + accessible name; closes the intermittent `/admin/audit` `scrollable-region-focusable` and benefits every table | [MAINTENANCE.md → Watch](MAINTENANCE.md#watch-items-known-tracked-deliberately-not-done) | Promoted from Watch (standard shadcn remedy, named there). UI +0.5. Effort S. |
| B3 | Calendar / UX | **Invitee-side signal when email is unconfigured** — post-F6, an invitee who cannot verify sees an empty `/calendar/invites`, no notification and no email, with nothing saying why. Add empty-state copy on the invites list, or a composer hint pointing at the per-guest RSVP link, gated on `!isEmailConfigured()` | [CHANGELOG](../CHANGELOG.md) (F6 entry) · [FEATURES.md](FEATURES.md) | **Filed 2026-08-05 by F6's contrarian pass** — deliberately not built inside an auth fix: it is an i18n copy surface (every string needs all locales), and the capability channel it should point at (the organizer's copyable RSVP link) already exists. "Features light up only when configured" is the standing agreement; this is the one place the calendar goes half-lit silently. Calendar/UX. Effort S. |
| B3 | UI / uploads | **Gate the avatar uploader on config** (or style the `readying` state accessibly) — a keyless deploy shows a perpetual 2.54:1 button; the a11y scan now excludes that state, but users still see it | [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) | UI +0.5. Effort S. |
| B3 | Docs / showcase | **README calendar currency** — the front door names "calendar" once (the Layout line, since 2026-08-26) while the status blurb + feature enumeration still omit it; add it to both. Pairs with the deck row below and the B3 reframe | [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) (F9) | Docs & DX +0.5 (re-priced 2026-09-01 — the Layout line landed 08-26). Effort S. |
| B3 | Docs / release | **Publish the v1.0.0 + v1.1.0 releases** — both are `draft=true` (`published_at: null`) since 2026-07-20 while the CHANGELOG header claims "each milestone is tagged … with a matching GitHub Release"; the public Releases page shows only v1.2.0 | [CHANGELOG](../CHANGELOG.md) · [releases](https://github.com/jrittelmeyer/next-web-boilerplate/releases) · [audit 09-01 F5](archive/PROJECT_AUDIT_2026-09-01.md) | Filed 2026-09-01. Owner one-click ×2: `gh release edit v1.0.0 --draft=false --latest=false` (and v1.1.0) — **`--latest=false` matters**: a newly published release defaults to "Latest" and would steal the badge from v1.2.0. Tags are public already, so the compare links work either way. Fallback if the drafting was deliberate: reword the CHANGELOG sentence to "from v1.2.0 on". Docs & DX +0.5. Effort XS. |
| B3 | UI / testing | **Stories for `select` + `form`** — the two `@repo/ui` primitives with no co-located `*.stories.tsx` (16 components, 13 stories — corrected 2026-09-01; the third story-less file, `theme-provider`, is a provider, not a primitive), violating the leaf rule `packages/ui/AGENTS.md` states; add both, or soften the leaf rule if story-less primitives are the honest posture | [packages/ui/AGENTS.md](../packages/ui/AGENTS.md) | Filed 2026-08-06 by the doc audit. A story-less primitive also never enters the visual-baseline set (`visual.spec.ts` screenshots stories only). Effort S. |
| B3 | Docs / context | **Context-doc splits per the repo's own budget** — `contextBudget.contextDocSplitTokens: 3000` vs `DATABASE.md` (53 KB), `DEPLOYMENT.md` (44 KB), `TESTING.md` (42 KB), `DECISIONS.md` (41 KB); each an index-stub + per-topic split on the AUTH.md precedent (2026-07-23). `docs/MAINTENANCE.md` (62 KB) cited from AGENTS.md as "the standing state" is the same class | [.claude/ai-dev-kit.config.json](../.claude/ai-dev-kit.config.json) · [CONVENTIONS.md](context/CONVENTIONS.md) | Filed 2026-08-25 by the fleet audit (kit `docs/archive/FLEET_UPGRADE_PLAN_2026-08-25.md`). On-demand docs, so cost-per-load not always-loaded — batch when touching each area. Effort M. |

### Shipped (strikethrough record)

Shipped record: [PROJECT_STATUS.md](PROJECT_STATUS.md) summary · full rows:
[archive/PHASE_HISTORY.md](archive/PHASE_HISTORY.md#backlog-shipped-row-archive-moved-2026-07-23)
(archived 2026-07-23). Future shipped rows keep **one strikethrough line** here —
don't re-expand them — until the next archive sweep.

| Band | Upgrade | Shipped | See |
| --- | --- | --- | --- |
| B2 | ~~Rate-limit completeness — `deleteCalendar` limiter (10/min; the six-step contract's missing step 2) + the `/rsvp` DB read capped 60/min per invitation on `loadRsvpView`, not the no-DB route handler; null-on-denial keeps the 200-page invariant~~ | 2026-08-08 | [CHANGELOG](../CHANGELOG.md) · [invitations.md](context/calendar/invitations.md) |
| B2 | ~~Assertion-blind-spot sweep — F2's two predicates closed from both sides (spelling pins, red-proven + the standing planted defects) plus four behavioral e2e sensors incl. the `calendar.range` ownership gate; remaining weak sensors filed as the B3 long-tail row~~ | 2026-08-08 | [CHANGELOG](../CHANGELOG.md) · [TESTING.md](context/TESTING.md) |
| B1 | ~~Advisory batch #5 (issue #41): ranged undici + socket.io-parser overrides, postcss key → 8.5.23, fast-uri GHSA parked (promoted on schedule 08-07)~~ | 2026-08-04 | [Watch](MAINTENANCE.md#watch-items-known-tracked-deliberately-not-done) · [CHANGELOG](../CHANGELOG.md) |
| B1 | ~~F4 — external guests never received cancellation emails (`or(isNull, ne)` + real-PG recipient-set tests)~~ | 2026-08-05 | [CHANGELOG](../CHANGELOG.md) · [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) |
| B1 | ~~F5 — `deleteEventSchema` refuses the scope/recurrenceId half-pair (shared `scopePairIssues`; first scope-pair tests)~~ | 2026-08-05 | [CHANGELOG](../CHANGELOG.md) · [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) |
| B2 | ~~F6 — the verified-email conjunct binds at both attendee writer seams (invite-time resolution + the respond UPDATE's `EXISTS`); no-backfill position on legacy stamps recorded~~ | 2026-08-05 | [CHANGELOG](../CHANGELOG.md) · [attendees.md](context/calendar/attendees.md) |
| B2 | ~~F7 — the `overlaps` seek reaches back a full occurrence span (opt-in `seekBackDays` = per-master span + zone slack; completeness tests, span > 2 periods)~~ | 2026-08-06 | [CHANGELOG](../CHANGELOG.md) · [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) |
| B2 | ~~F8 — `YEARLY;BYMONTHDAY` without `BYMONTH` expands every month; corpus samples the unpaired family (+40 → 568, SHA repinned, census zero)~~ | 2026-08-06 | [CHANGELOG](../CHANGELOG.md) · [audit 08-04](archive/PROJECT_AUDIT_2026-08-04.md) |
| B1 | ~~Kit / CI — kit-gate Biome defect: installer's `settings.json` output reformatted by Biome; closed consumer-side (installer-owned paths excluded in `biome.json`)~~ | 2026-08-25 | [CHANGELOG](../CHANGELOG.md) · `32dd92a` |
| B1 | ~~Kit / hooks — `compact-reorient.mjs` nudge survives `--slim`; `--hooks` adopted (fix landed kit-side in 0.23.1)~~ | 2026-08-25 | [CHANGELOG](../CHANGELOG.md) · `32dd92a` |
| B2 | ~~Testing / tooling — `docs-sanity.mjs` hook checks widened to exec form; settings.json ↔ hooks.json kit-wiring parity check added~~ | 2026-08-25 | [CHANGELOG](../CHANGELOG.md) · `32dd92a` |
| B3 | ~~Doc-audit 2026-08-26 leftovers (template surface) — `pnpm clean` real scripts, dangling contrarian-pointer fix, Playwright rule completeness, kit `exactPin`+`better-auth` (kit reinstalled 0.23.1→0.23.11, drift surfaced), skill-description budget re-verified under cap~~ | 2026-08-29 | [CHANGELOG](../CHANGELOG.md) |
| B3 | ~~Cut v1.2.0 — CHANGELOG `[Unreleased]` rolled into a dated `[1.2.0]` (condensed Added/Changed/Fixed/Security + compare link; detail stays in PHASE_HISTORY); tag + GitHub Release; CI green on the tag~~ | 2026-08-30 | [CHANGELOG](../CHANGELOG.md) · [release](https://github.com/jrittelmeyer/next-web-boilerplate/releases/tag/v1.2.0) |
| B1 | ~~Kit 0.23.11 → 0.23.16 + adapter contract — 13 drifted files (7 skills gain `disable-model-invocation`, `stop-gate` → `asyncRewake`, settings-aware inventory, refreshed `sources.md`/`stack.md`); adapter moved from legacy `prodVerify` to the schema's `verify` block with the `run` string **verified by running both forms**; `harnessAudit.kitSourcePath` deliberately dropped (local-path leak in a public template); riders a–g landed, incl. the `docs:sanity` parity check widened to deep-equality and red-proven~~ | 2026-09-02 | [CHANGELOG](../CHANGELOG.md) · [CONVENTIONS.md → Agent tooling](context/CONVENTIONS.md#agent-tooling-claude) |
| B1 | ~~Fork-safe gate for `renovate.yml` — job-level `if: ${{ vars.ENABLE_RENOVATE == 'true' }}`, the `ENABLE_CODEQL`/`ENABLE_VISUAL` convention; unset here and in every generated project ⇒ the lane skips silently instead of failing at startup every Monday. Rides with a dated 14-day liveness check, since enabling is now a two-action close~~ | 2026-09-02 | [renovate.yml](../.github/workflows/renovate.yml) · [MAINTENANCE.md](MAINTENANCE.md#automation-on-a-fork--new-repo) · [audit 09-01 F2](archive/PROJECT_AUDIT_2026-09-01.md) |
